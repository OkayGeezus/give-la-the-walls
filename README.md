# Give Los Angeles the Walls

Single-page public-art proposal site for Guillermo Palacio.

## Develop

```bash
cd ~/Github/give-la-the-walls
npm install
npm run dev
```

## Build

```bash
npm run build
npm run preview
```

## Update content

Edit `src/data/siteContent.ts` for name, contact, Instagram, image alt text, and captions.

Drop photography into `public/images/` — see `public/images/README.md`. Missing images show placeholders automatically.
